interface:
  display_name: "Herdr Agent Dispatch"
  short_description: "Launch and collect Herdr background agents"
  default_prompt: "Use $herdr to launch a background agent with a durable handoff."
policy:
  allow_implicit_invocation: false
