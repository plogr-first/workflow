# Candidate Contract

Before reporting `candidate`, persist non-empty `result.md` and valid `outcome.json`.

Include: acceptance checks and results, changed files, worktree decision and path, branch, base SHA, candidate SHA, commands and outcomes, remaining limitations, and confirmation that no secrets were recorded. The task agent does not merge.
