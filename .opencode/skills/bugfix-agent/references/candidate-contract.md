# Candidate Contract

Before reporting `candidate`, persist non-empty `result.md` and valid `outcome.json`. Include the audit location and mechanism, RED and GREEN commands, regression results, changed files, worktree decision and path, branch, base SHA, candidate SHA, and any blocker. The bugfix agent does not merge.
