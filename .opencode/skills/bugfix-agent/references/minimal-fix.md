# Minimal Complete Fix

Change only the confirmed root-cause seam and the regression check required to prove it. Preserve existing style and unrelated user changes. Do not broaden the patch into cleanup or refactoring. Remove temporary logs, probes, and fixtures before candidate handoff.
